package net.openhft.chronicle.queue;

import net.openhft.chronicle.core.util.Ints;
import org.junit.Test;

public class ZeroCostTripWireTest {

    @Test
    public void trip() {
        System.out.println("Ints.assertIfEnabled(i -> i > 0, -1) = " + Ints.assertIfEnabled(i -> i > 0, -1));
    }

}
